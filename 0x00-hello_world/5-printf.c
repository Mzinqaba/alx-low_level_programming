#include <stdio.h>
/**
 * main -Using printf function instead of puts to print a line
 * Return:Returns(0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
